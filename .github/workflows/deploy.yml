# File Path: .github/workflows/deploy-react.yml
name: Deploy React App to FTP Server

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    env:
      VITE_ALATPAY_API_KEY: 538cf1d3cbb041ca89a48eb942e30961
      VITE_ALATPAY_BUSINESS_ID: 3076f833-25a7-4903-dff2-08dc653aeecb

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Create a new directory
        run: mkdir -p build

      - name: Verify the directory was created
        run: ls -la

      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: "20.11.1"

      - name: Install dependencies
        run: npm install

      - name: Build the React app
        run: npm run build

      - name: 📂 Sync files via FTP
        uses: SamKirkland/FTP-Deploy-Action@4.0.0
        with:
          server: ftp.foodsbymomi.com
          username: fooddjkb
          password: 5LTq0qvzHhv4
          server-dir: public_html/
          local-dir: dist/
          protocol: ftp
